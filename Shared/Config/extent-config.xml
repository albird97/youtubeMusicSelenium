<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
	<configuration>
		<!-- report theme -->
		<!-- STANDARD, DARK -->
		<theme>STANDARD</theme>

		<!-- document encoding -->
		<!-- defaults to UTF-8 -->
		<encoding>UTF-8</encoding>

		<!-- protocol for script and stylesheets -->
		<!-- defaults to https -->
		<!-- HTTP, HTTPS -->
		<protocol>HTTPS</protocol>

		<!-- timeline report -->
		<timelineEnabled>false</timelineEnabled>

		<!-- offline report -->
		<enableOfflineMode>true</enableOfflineMode>

		<!-- use thumbnails for base64 images -->
		<!-- this may slowdown viewing tests -->
		<thumbnailForBase64>false</thumbnailForBase64>

		<!-- title of the document -->
		<documentTitle>Mobile Automation Report</documentTitle>

		<!-- report name - displayed at top-nav -->
		<reportName>Mobile Automation Report</reportName>

		<!-- report headline - displayed at top-nav, after reportHeadline -->
		<reportHeadline>YoutubeMusic</reportHeadline>

		<!-- timestamp format -->
		<timeStampFormat>MMM dd, yyyy HH:mm:ss</timeStampFormat>

		<!-- custom javascript -->
		<scripts>

			<![CDATA[
                $('.nav-logo').text('YoutubeMusic');
                $('#test-view > div.subview-left.left > div').find('h5').text('Test cases');
                $('#category-view > div.subview-left.left > div').find('h5').text('Test suites');
            ]]>

		</scripts>
		<styles>

			<![CDATA[

            .report-name { padding-left: 480px; } .report-name > img { float: left;height: 100%;margin-left: 120px;margin-top: 3px;width: auto; }
            nav .brand-logo { font-size: 15px; padding-left: 6px; position: relative; margin-right: 20px; font-weight: 600; width: 70px;padding-top: 1px; }
            nav .report-name { font-size: 30px; color: #49b3e8;}
            nav .nav-wrapper { background-color: white;}
        ]]>

		</styles>
	</configuration>
</extentreports>